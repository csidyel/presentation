version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e2-standard-2
    os_image: ubuntu2004
global_job_config:
  env_vars:
    - name: SEMAPHORE_GIT_DEPTH
      value: '1'
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #'
          commands:
            - sem-service start elasticsearch 8.11.3
      secrets:
        - name: buildkite
