version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-4
    os_image: ubuntu2004
  containers:
    - name: main
      image: 'registry.semaphoreci.com/android:31-flutter'
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #'
          commands:
            - openssl version
    run:
      when: change_in('/lib')
