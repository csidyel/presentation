version: v1.0
name: CI Pipeline
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Frontend Cache (test)
    task:
      jobs:
        - name: Build Ember Cache
          commands:
            - sleep 30
            - |
              if (( RANDOM % 2 == 0 )); then
                echo "Command succeeded";
              else
                echo "Command failed";
                return 1;
              fi
    dependencies: []
